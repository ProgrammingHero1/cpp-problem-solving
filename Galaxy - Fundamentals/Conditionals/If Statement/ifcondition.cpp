#include<iostream>
using namespace std;
int main()
{
    // if it is Friday today, I will not go to school
    // in programming, the "if" does the same thing. The first part
    // of the sentence (it is friday)  goes as a condition and
    // the second one (I will not goes to school) is a statement
    
    //let us declare an integer variable named "n"
    
    int n;
    n=10;
    
    if(n==10)
    {
        cout<<"The program has an integer number which is 10.\n";
    }
    
    return 0;
}
